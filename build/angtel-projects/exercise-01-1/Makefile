APP_NAME ::= exercise-01-1

RELATIVE_PATH_PREFIX ::= ..
COMMON_MAKEFILE_NAME ::= common.mk
MAIN_MAKEFILE ::= $(RELATIVE_PATH_PREFIX)/$(COMMON_MAKEFILE_NAME)

ifneq ($(KERNELRELEASE),)
MAIN_MAKEFILE ::= $(M)/$(MAIN_MAKEFILE)
endif

include $(MAIN_MAKEFILE)
